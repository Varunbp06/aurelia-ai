{
  "name": "@aurelia/widget",
  "version": "1.0.0",
  "description": "Aurelia AI Chat Widget - 可嵌入的智能聊天组件",
  "main": "dist/aurelia-widget.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run typecheck && npm run build:dev && npm run build:prod",
    "build:dev": "esbuild src/AureliaWidget.tsx --bundle --format=esm --outfile=dist/aurelia-widget.js --sourcemap",
    "build:prod": "esbuild src/AureliaWidget.tsx --bundle --format=iife --outfile=dist/aurelia-widget.min.js --minify --sourcemap",
    "dev": "esbuild src/AureliaWidget.tsx --bundle --format=iife --outfile=example/aurelia-widget.js --servedir=example",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "ai",
    "chatbot",
    "widget",
    "customer-service",
    "aurelia"
  ],
  "author": "Aurelia Team",
  "license": "MIT",
  "devDependencies": {
    "@types/jsdom": "^28.0.3",
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.12",
    "jsdom": "^29.1.1",
    "typescript": "^5.3.0",
    "vitest": "^4.1.4"
  }
}
